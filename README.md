# trsh : now you can remove files carefree.
`trsh` will send your files to a temporary location `.trash` where files stay for a certain time period.You can clean up your `.trash` by calling `trsh --clean`
# Quick Start
```console
$ make
$ ./trsh
```
